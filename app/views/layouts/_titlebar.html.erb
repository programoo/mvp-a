<div class="container-fluid bg-light d-flex flex-row p-2 mt-1 sticky-top">
  <div class="col-6">
    <%= yield :level_one %>
    <% if content_for?(:level_two) %>
      >
      <%= yield :level_two %>
    <% end %>
    <% if content_for?(:level_three) %>
      >
      <%= yield :level_three %>
    <% end %>
  </div>
  <div class="col-6">
    <%= yield :page_title %>
  </div>
</div>